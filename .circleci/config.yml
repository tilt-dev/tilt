version: 2.1
orbs:
  win: circleci/windows@2.2.0
jobs:
  build-windows:
    executor:
      name: win/default
      size: "medium"
    steps:
      - run: choco install make

workflows:
  version: 2
  build:
    jobs:
      - build-windows